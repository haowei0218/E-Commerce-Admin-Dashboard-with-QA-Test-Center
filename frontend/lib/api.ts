import { Response } from "@/type/api.response.type";
import { UserLoginPayload } from "@/type/user.login.type";

export async function userLogin({ account, password }: UserLoginPayload): Promise<Response['userLogin']> {
    try {
        
    } catch (error) {

    }
}